```
event_d.c
↓
settlement_d.c（只做 minxiong，六維 + save/load）
↓
minxiong.yaml（靜態資料）
↓
footprint_d.c（只做地理踏印的 grant + has_footprint）
↓
world.c 加入 domain_id + check_site_at
↓
第一個可以玩的場景：走進民雄老車站 → 獲得踏印 → 民雄記憶值 +5
```
